[Unit]
Description=Cloudflare Tunnel for DVI Backend
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/env cloudflared tunnel run dvi-backend
Restart=always

[Install]
WantedBy=multi-user.target
